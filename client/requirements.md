## Packages
recharts | Data visualization for academic progress and stress trends
framer-motion | Smooth animations for page transitions and micro-interactions
lucide-react | Icons for UI elements (already in base, but listing for completeness)

## Notes
Tailwind Config: Extended colors for dark mode, gradients, and specific Web3-style accents.
Fonts: DM Sans (Body) and Outfit (Headings) via Google Fonts.
Auth: Session-based auth with `credentials: "include"`.
